# Notify when adjusted pool water level drops below threshold.

- id: pool_low_water_email
  alias: Notify when pool water level is low
  description: >
    Sends email and/or phone when pool adjusted water level stays below
    threshold for 2 minutes, if the alert and channel helpers are on.
  mode: single
  trigger:
    - platform: template
      value_template: >-
        {% set raw = states('sensor.water_depth_sensor_distance') %}
        {% if raw in ['unknown', 'unavailable', 'none', ''] %}
          false
        {% else %}
          {% set measured = raw | float(none) %}
          {% if measured is none %}
            false
          {% else %}
            {% set unit = state_attr('sensor.water_depth_sensor_distance', 'unit_of_measurement')
               | default('ft') | lower %}
            {% if unit in ['in', 'inch', 'inches'] %}
              {% set inches = measured %}
            {% elif unit in ['m', 'meter', 'meters'] %}
              {% set inches = measured * 39.3701 %}
            {% elif unit in ['cm', 'centimeter', 'centimeters'] %}
              {% set inches = measured / 2.54 %}
            {% else %}
              {% set inches = measured * 12 %}
            {% endif %}
            {% set offset = states('input_number.pool_water_level_offset') | float(0) %}
            {% set adjusted = inches - offset %}
            {% set threshold = states('input_number.pool_low_water_inches') | float(-1) %}
            {{ adjusted < threshold }}
          {% endif %}
        {% endif %}
      for:
        minutes: 2
  condition:
    - condition: state
      entity_id: input_boolean.pool_low_water_email_enabled
      state: 'on'
    - condition: or
      conditions:
        - condition: state
          entity_id: input_boolean.dashboard_notify_email_enabled
          state: 'on'
        - condition: state
          entity_id: input_boolean.dashboard_notify_phone_enabled
          state: 'on'
  action:
    - service: script.dashboard_log_control
      data:
        source: automation
        actor: pool_low_water_email
        action: notify.alert
        entity_id: sensor.water_depth_sensor_distance
        ok: '1'
        detail_b64: '-'
    - parallel:
        - if:
            - condition: state
              entity_id: input_boolean.dashboard_notify_email_enabled
              state: 'on'
          then:
            - service: script.dashboard_notify_email
              data:
                title: Pool water level low
                message: >-
                  Pool water level is below {{ states('input_number.pool_low_water_inches') }} in
                  (threshold). Check the home dashboard Pool widget.
        - if:
            - condition: state
              entity_id: input_boolean.dashboard_notify_phone_enabled
              state: 'on'
          then:
            - service: script.dashboard_notify_phone
              data:
                title: Pool water level low
                message: >-
                  Pool water level is below {{ states('input_number.pool_low_water_inches') }} in.
